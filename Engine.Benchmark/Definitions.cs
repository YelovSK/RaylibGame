using System.Numerics;

namespace Engine.Benchmark;

struct CompA { public Vector2 A, B, C; }
struct CompB { public Vector2 A, B, C; }
struct CompC { public Vector2 A, B, C; }
struct CompD { public Vector2 A, B, C; }
struct CompE { public Vector2 A, B, C; }
